export class ContactItem {
    data;
    link;
    icon;

    constructor(data: string, link: string, icon: string) {
        this.data = data;
        this.link = link;
        this.icon = icon;
    }
}
